syntax = "proto3";

message Data {
  string name = 1;
  int64 timestamp = 2;  // Epoch time in seconds
  Metadata metadata = 3;
  string experiment_class = 4;
  bytes file = 5;

  // Nested message for 'system' (can be further defined as needed)
  message Metadata {
  }
}