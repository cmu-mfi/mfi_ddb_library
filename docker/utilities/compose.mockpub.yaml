version: '3.8'

services:
  mock-publisher:
    build: .
    container_name: mock-publisher
    restart: unless-stopped
    volumes:
      # Mount config.yaml so users can edit settings without rebuilding the Docker image
      - ./config.yaml:/app/config.yaml:ro
    profiles:
      - "mock-publisher"